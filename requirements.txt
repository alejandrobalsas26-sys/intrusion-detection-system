# Environment variable loading for application entrypoint (Strict Pinning)
python-dotenv==1.2.2

# Network sensor module (L2)
scapy==2.7.0

# Authentication module (L7) - cryptographic primitives
cryptography==48.0.1
pyotp==2.9.0
qrcode==8.2

# Dashboard module (L8) - web framework + security
Flask==3.1.3
Flask-Talisman==1.1.0
Flask-WTF==1.2.1

# Production WSGI server (Windows-compatible; gunicorn is POSIX-only)
waitress==3.0.2

# Testing framework (formal adoption post-rogue-execution)
pytest==9.0.3
